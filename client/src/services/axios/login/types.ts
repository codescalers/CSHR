export type loginDataType = {
  email: string;
  password: string;
};

export type loggingData = {
  refresh_token: string;
  access_token: string;
};

export type refreshData = {
  refresh: string;
  access: string;
};
